// Package main implements the fantasy-football-engine daemon entry point.
// It loads configuration, initializes telemetry, and runs the scheduler,
// corpus pipeline, and MCP server concurrently.
package main
